# Step 4B: Execute, verify, and prove

```text
For the live workshop, use the included CSV lab. Approved. Run the plan using read-only sources.

1. Calculate the last two complete weeks from the calculation source.
2. Reconcile them to the independent benchmark.
3. Show percentage-point and relative change separately.
4. Decompose by channel, device, and landing page; run an isolation check.
5. Run verify.py as a second method when Python is available. Compare every quantitative claim as Pass, Warning, or Fail.
6. Revise any failed claim before continuing.

Return exactly one outcome: Answer, Clarify, Review, or Refuse. Include the question, source execution proof, context used, validations passed and failed, uncertainty, caveat, next action, and human owner. Save work/04_proof.yaml.

Do not call an observed contributor a root cause unless the evidence isolates causality.
```

For the clean lab, expect a Review state. A tagging change prevents an automatic Answer even though the calculations and benchmark tie-out pass.
